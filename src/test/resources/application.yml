spring:
  datasource:
    url: jdbc:h2:mem:peticionamento;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    
flyway:
  validate-on-migrate: false